# MySQL profile for running against a local/remote MySQL server
# Activate with -Dspring.profiles.active=mysql or set SPRING_PROFILES_ACTIVE=mysql

server.port=8081

spring.datasource.url=jdbc:mysql://localhost:3306/studio?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=KHUSHAL#0812
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway (optional) - enable if you add migrations under db/migration
spring.flyway.enabled=false

# Recommended connection pool tuning for production (adjust as needed)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
